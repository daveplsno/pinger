version: "3.5"

services:
  pong:
    restart: always
    container_name: pong
    image: node:lts-slim
    #build: .
    working_dir: /app/pong
    command: yarn start
    volumes:
      - ./data:/app
    ports:
      - "3000:3000"
      - "5000:5000"
    stdin_open: true
